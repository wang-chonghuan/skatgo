.git
node_modules
app/node_modules
dist
app/dist
.output
app/.output
app/.tanstack
npm-debug.log

# secrets — never into the image (matches .env and any .env.* at any depth)
**/.env
**/.env.*

# Build-context slimming: `az acr build` uploads the whole repo root, and none of
# these participate in the app image.
.tmp
tmp
test-results
playwright-report
