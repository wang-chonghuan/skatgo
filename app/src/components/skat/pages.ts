// The three page bodies of the course, behind one module so the routes can load the whole course as
// a single browser-only chunk — see ./client-page.tsx for why none of it is server-rendered.
export { CourseHome } from './course-home'
export { FreePlay } from './free-play'
export { LessonPage } from './lesson-page'
